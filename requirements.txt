selenium>=4.0.0
webdriver-manager>=4.0.0
psutil>=5.9.0
watchdog>=3.0.0
pyinstaller>=6.0.0

# Note: FFmpeg must be installed separately for audio tag overlay feature
# Download from: https://ffmpeg.org/download.html
# Or via package manager: apt install ffmpeg (Linux), winget install ffmpeg (Windows)
